# Legacy local YOLO dataset config.
# Roboflow is the authoritative dataset, training, and deployment platform for
# this prototype. Keep this file only if a future local/edge export is needed.

path: ../../data/processed   # root dir (relative to this file)
train: images/train
val: images/val
test: images/test            # optional, only used for final eval

# Class list — adjust to match what you finalize in Roboflow
names:
  0: debris_net_or_pot
  1: pipe_cylinder
  2: shipwreck
  3: unknown_anomaly

# Do not use this file for the current hosted-training workflow.
